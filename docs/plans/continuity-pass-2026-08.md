# Continuity pass (2026-08) — nc-litter

## Changes
- Redrew `img/app.svg` as single-ink `currentColor` cat/litter silhouette.
- Scoped brand/spacing tokens off `:root` onto `#nc-litter-root` / `#nc-litter-admin` / `.nc-litter-mount` (roomba pattern) to stop Settings-page token bleed.
- Nav order → 87.

## Verify
- Settings page with litter + roomba admin sections: no cross-app token bleed.
- Nav icon monochrome.
